name: "NPM package"

on:
  workflow_dispatch:
    inputs:
      newVersion:
        description: "Add version: v[MAJOR].[MINOR].[PATCH]"
        required: true
        default: 'v0.0.0'

env:
  NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
  GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: "⚙ Set up Node.js"
        uses: actions/setup-node@v2
        with:
          node-version: '14.x'
      - name: "✅ Checkout repository"
        uses: actions/checkout@v2

  publish:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: "💾 Restore packages from cache"
        uses: actions/cache@v2
        with:
          path: |
            node_modules
            */*/node_modules
          key: ${{ runner.os }}-${{ hashFiles('**/package-lock.json') }}
      - name: "📦 Install packages"
        run: npm install
      - name: "🔦 Lint"
        run: npm run lint
      - name: "🔨 Build"
        run: npm run build
      - name: "👨‍💻 Run Test"
        run: npm run test
        env:
          NODE_ENV: 'test'
      - name: "🎉 Publish pkg: ${{github.event.inputs.newVersion}}"
        run: |
          #${{ github.event.release.tag_name }}
          #${{ github.event.release.tag_name }}
          git config --global user.name "GitHub CD bot"
          git config --global user.email "mdelgado@tresdoce.com.ar"
          npm version ${{github.event.inputs.newVersion}}
          cd ./pkg
          npm config set '//registry.npmjs.org/:_authToken' "${NODE_AUTH_TOKEN}"
          npm publish
      - run: git push

